name: Get Unity Activation File
# This workflow is deprecated — game-ci/unity-request-activation-file@v2
# no longer works and Unity 6.x does not create a .ulf file.
# Kept here for reference only. Use build-ios.yml directly.

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "This workflow is no longer needed. See build-ios.yml."
